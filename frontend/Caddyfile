:3000 {
    root * /usr/share/caddy
    file_server
    
    # Proxy ke backend via network internal Docker
    handle_path /api/* {
        reverse_proxy api-preventive-tahuna:3000
    }

    try_files {path} /index.html
}